# PRODUCTION — copy variable NAMES into Render Dashboard (Environment).
# Never put real passwords in this file or in git.

PORT=5000
CLIENT_ORIGIN=http://localhost:5173,https://your-site.netlify.app

# Paste full Atlas connection string only in Render (not here)
MONGODB_URI=

ACCESS_TOKEN_SECRET=
REFRESH_TOKEN_SECRET=
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d

EMAIL_ENABLED=true
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
